[build]
builder = "nixpacks"
buildCommand = "npm install --prefix client && CI=false npm run build --prefix client && npm install --prefix server"

[deploy]
startCommand = "npm start --prefix server"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
