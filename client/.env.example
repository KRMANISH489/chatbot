# Local development — copy to .env
# Local dev only. On Railway frontend service set this to your backend URL, e.g.:
# REACT_APP_API_URL=https://your-api.up.railway.app
REACT_APP_API_URL=http://localhost:5000
